lenovo-logo.png - the official Lenovo logo (red badge, provided by the
organizer). Not currently used anywhere in the app (the floating
bottom-right badge that used it was removed) - kept here in case it's
needed again later.

The header uses text, not an image - components/Logo.tsx renders "Lenovo"
as styled text (in the brand red) next to "LEAP HACKATHON".
